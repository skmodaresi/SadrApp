<UserControl x:Class="SadrApp.Views.Controls.CategoryTreeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="10" Margin="0,0,0,8">
      <StackPanel Orientation="Horizontal">
        <Button x:Name="BtnAddRoot" Content="➕ ریشه جدید" Style="{StaticResource PrimaryBtn}" Margin="0,0,6,0"/>
        <Button x:Name="BtnAddChild" Content="↳ زیرشاخه" Style="{StaticResource GhostBtn}" Margin="0,0,6,0"/>
        <Button x:Name="BtnRename" Content="✏️ تغییر نام" Style="{StaticResource GhostBtn}" Margin="0,0,6,0"/>
        <Button x:Name="BtnDelete" Content="🗑 حذف" Style="{StaticResource DangerBtn}" Margin="0,0,6,0"/>
        <Button x:Name="BtnAttribs" Content="🔗 ویژگی‌ها" Style="{StaticResource GhostBtn}" Margin="0,0,6,0" ToolTip="اتصال ویژگی‌ها به دسته انتخاب‌شده"/>
        <Button x:Name="BtnReload" Content="🔄 بازخوانی" Style="{StaticResource GhostBtn}"/>
      </StackPanel>
    </Border>

    <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="8">
      <TreeView x:Name="Tree"
                FontSize="13"
                BorderThickness="0"
                Background="Transparent"
                VirtualizingPanel.IsVirtualizing="True">
        <TreeView.ItemContainerStyle>
          <Style TargetType="TreeViewItem">
            <Setter Property="IsExpanded" Value="True"/>
            <Setter Property="Padding" Value="4,3"/>
            <Setter Property="Foreground" Value="#22314F"/>
          </Style>
        </TreeView.ItemContainerStyle>
        <TreeView.Resources>
          <HierarchicalDataTemplate DataType="{x:Type local:CategoryNode}"
                                    ItemsSource="{Binding Children}"
                                    xmlns:local="clr-namespace:SadrApp.ViewModels">
            <StackPanel Orientation="Horizontal">
              <TextBlock Text="📁" Margin="0,0,6,0"/>
              <TextBlock Text="{Binding Name}"/>
              <TextBlock Text="{Binding Code, StringFormat='  ({0})'}" Foreground="#8A94A8"/>
            </StackPanel>
          </HierarchicalDataTemplate>
        </TreeView.Resources>
      </TreeView>
    </Border>
  </Grid>
</UserControl>
